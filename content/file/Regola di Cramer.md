#uni 
Il vettore soluzione si può scrivere componente per componente come:$$x_j=\frac{det(A_j)}{det(A)} $$
dove $A_j$ è la matrice ottenuta da $A$ sostituendo la colonna $j$ con $b$.
Questa procedura ha una complessità computazionale pari a calcolarsi n determinanti di matrici $(n\times n)$.