#self-taught 
Secure Shell.

Host:
```bash
systemctl status sshd # to check (could be ssh.service)

sudo systemctl enable sshd
sudo systemctl start sshd
ip addr
```

Client:
```Bash
ssh user@hostname
```